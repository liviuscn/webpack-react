import React, { useEffect } from 'react'
import { Layout } from 'antd'
const { Content } = Layout;
export default (props) => {
    useEffect(() => {
        //request商品详情

    })
    return <Layout>
        <Content>详情</Content>
    </Layout>
}