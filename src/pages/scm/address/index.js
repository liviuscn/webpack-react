import React from 'react';
import NavBar from 'pdv/navBar'
import './index.less'

export default () => {
    return <div className="address-container">
        <NavBar>收货地址</NavBar>
        <div>新增</div>
    </div>
}