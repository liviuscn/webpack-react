import React, { useState, useEffect, Fragment } from "react";
import { Transition, CSSTransition, TransitionGroup } from 'react-transition-group';
import './index.less'

export default(props)=>{
    return <div className="pdv-spin">
        <div className="spin"></div>
    </div>
}