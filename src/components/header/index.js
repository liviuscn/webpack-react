import React, { useState, useEffect } from "react";
import styles from './header.less'

export default (props) => {
    return (<header>
        <span>back</span>
        <span>title</span>
        <span></span>
    </header>)
}